# PetBeats Premium Subscription Page 기획서

## 📊 가격 정책 (Pricing Strategy)

### 목표
사용자가 **1년권(Yearly)** 을 선택하도록 유도

### 가격표

| 플랜 | 가격 | 월 환산 | 할인율 | 비고 |
|------|------|---------|--------|------|
| 1개월 (Monthly) | ₩3,900 | ₩3,900 | - | 기준가 |
| 3개월 (Quarterly) | ₩10,900 | ₩3,633 | 7% | - |
| 6개월 (Half-Year) | ₩19,900 | ₩3,316 | 15% | - |
| **1년 (Yearly)** | **₩33,000** | **₩2,750** | **30%** | 👑 **BEST OFFER** |

**공통 혜택**: 모든 플랜 **첫 7일 무료 체험** 제공

---

## 🎨 UI 레이아웃 및 디자인

### Section A: 헤더 (Value Proposition)

#### 배경 이미지
- 편안하게 자는 강아지/고양이 고퀄리티 이미지
- 또는 부드러운 그라데이션 배경

#### 메인 카피
```
한국어: 반려동물에게 완벽한 평온을 선물하세요.
English: Unlock Complete Calm for Your Pet.
```

#### 서브 카피 (혜택 요약)
```
✓ 90개 전용 테라피 사운드 무제한
✓ Smart Sync (날씨/리듬/시터) 잠금 해제
✓ Haptic (진동 테라피) 무제한 사용
```

**디자인 요구사항**:
- 메인 카피: 28sp, Bold, 흰색
- 서브 카피: 14sp, Medium, 흰색 80% 투명도
- 체크마크 아이콘: 파란색 또는 골드

---

### Section B: 플랜 선택 카드 (Plan Selector)

#### 레이아웃
세로형 리스트 (4개 카드)

#### Card 1: 1년 (Yearly) - 최우선 강조 🌟

**디자인 특징**:
- 테두리: 골드 그라데이션 (2px)
- 배경: 반짝이는 효과 또는 미묘한 그라데이션
- "BEST VALUE" 뱃지: 우측 상단 또는 좌측 상단

**카피**:
```
타이틀: 1년 (Yearly) 👑
가격: ₩33,000 /년
할인: 월 2,750원 (30% 할인)
특전: 7일 무료 체험
```

**Flutter 디자인 가이드**:
```dart
Container(
  decoration: BoxDecoration(
    gradient: LinearGradient(
      colors: [Color(0xFFFFD700), Color(0xFFFFE55C)],
    ),
    borderRadius: BorderRadius.circular(20),
    boxShadow: [
      BoxShadow(
        color: Colors.amber.withOpacity(0.3),
        blurRadius: 20,
        offset: Offset(0, 8),
      ),
    ],
  ),
  // ...
)
```

#### Card 2: 6개월 (6 Months)

```
타이틀: 6개월 (6 Months)
가격: ₩19,900 /6개월
할인: 15% Save
특전: 7일 무료 체험
```

**디자인**: 일반 카드 (흰색 배경, 연한 테두리)

#### Card 3: 3개월 (3 Months)

```
타이틀: 3개월 (3 Months)
가격: ₩10,900 /3개월
할인: 7% Save
특전: 7일 무료 체험
```

#### Card 4: 1개월 (Monthly)

```
타이틀: 1개월 (Monthly)
가격: ₩3,900 /월
할인: -
특전: 7일 무료 체험
```

---

### Section C: 친구 초대 넛지 (Referral Nudge)

#### 위치
플랜 선택 카드 바로 아래 또는 결제 버튼 아래

#### 카피
```
한국어: 💰 아직 결제가 망설여지시나요? 
        친구 초대하고 무료 이용권 받기 >

English: Not ready to subscribe? 
         Invite friends & get free access >
```

#### 동작
클릭 시 → 친구 초대 페이지 (`Routes.INVITE`) 이동

#### 디자인
```dart
TextButton(
  onPressed: () => Get.toNamed(Routes.INVITE),
  child: Row(
    mainAxisAlignment: MainAxisAlignment.center,
    children: [
      Text('💰 아직 결제가 망설여지시나요?'),
      SizedBox(width: 8),
      Text('친구 초대하고 무료 이용권 받기 >',
        style: TextStyle(
          color: AppColors.primaryBlue,
          fontWeight: FontWeight.bold,
        ),
      ),
    ],
  ),
)
```

---

### Section D: 결제 버튼 (CTA)

#### 버튼 텍스트
```
한국어: 7일 무료 체험 시작하기
English: Start My 7-Day Free Trial
```

#### 하단 설명 (Micro-copy)
```
한국어: 7일 동안 요금이 청구되지 않습니다. 언제든 해지 가능합니다.
English: Won't be charged until the trial ends. Cancel anytime.
```

#### 디자인
```dart
ElevatedButton(
  style: ElevatedButton.styleFrom(
    backgroundColor: AppColors.primaryBlue,
    padding: EdgeInsets.symmetric(vertical: 18.h),
    shape: RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(16.r),
    ),
    elevation: 8,
    shadowColor: AppColors.primaryBlue.withOpacity(0.4),
  ),
  onPressed: () => controller.startFreeTrial(),
  child: Text(
    '7일 무료 체험 시작하기',
    style: TextStyle(
      fontSize: 18.sp,
      fontWeight: FontWeight.bold,
      color: Colors.white,
    ),
  ),
)

// 하단 설명
Padding(
  padding: EdgeInsets.only(top: 12.h),
  child: Text(
    '7일 동안 요금이 청구되지 않습니다. 언제든 해지 가능합니다.',
    style: TextStyle(
      fontSize: 12.sp,
      color: Colors.grey,
    ),
    textAlign: TextAlign.center,
  ),
)
```

---

## 🔄 결제 로직 및 흐름 (Flow Logic)

### 1. 진입 경로

**트리거**:
- 잠금 기능(PRO 트랙, 날씨, 리듬, 시터) 클릭 시
- [PRO 업그레이드] 버튼 클릭 시
- 설정 > 프리미엄 메뉴 진입 시

**코드**:
```dart
// 어디서든 호출 가능
Get.toNamed(Routes.SUBSCRIPTION);
```

### 2. 플랜 선택

사용자가 4개 플랜 중 하나 선택:
```dart
class SubscriptionController extends GetxController {
  final selectedPlan = SubscriptionPlan.yearly.obs;
  
  void selectPlan(SubscriptionPlan plan) {
    selectedPlan.value = plan;
  }
}

enum SubscriptionPlan {
  monthly,
  quarterly,
  halfYearly,
  yearly,
}
```

### 3. 결제 시작

**버튼 클릭 시**:
```dart
Future<void> startFreeTrial() async {
  // 1. RevenueCat 또는 IAP 호출
  final offerings = await Purchases.getOfferings();
  final package = _getPackageForPlan(selectedPlan.value);
  
  // 2. 무료 체험 안내 다이얼로그
  final confirmed = await _showTrialDialog();
  if (!confirmed) return;
  
  // 3. 결제 시작
  try {
    await Purchases.purchasePackage(package);
    
    // 4. 성공 처리
    await _handlePurchaseSuccess();
  } catch (e) {
    // 에러 처리
    _showErrorDialog(e);
  }
}
```

### 4. 결제 모듈 호출

**인앱결제(IAP) 창 표시**:
```
오늘 결제금액: ₩0
7일 후 결제금액: ₩33,000 (1년권 선택 시)
```

### 5. 성공 처리

**즉시 잠금 해제**:
```dart
Future<void> _handlePurchaseSuccess() async {
  // 1. Premium 상태 업데이트
  isPremiumUser.value = true;
  
  // 2. GetStorage에 저장
  final storage = GetStorage();
  storage.write('isPremium', true);
  storage.write('subscriptionPlan', selectedPlan.value.toString());
  storage.write('subscriptionDate', DateTime.now().toIso8601String());
  
  // 3. 기능 잠금 해제
  // - Weather API 호출 허용
  // - Rhythm/Sitter 모드 활성화
  // - Premium 트랙 재생 허용
  
  // 4. 성공 메시지
  Get.snackbar(
    '구독 완료!',
    '모든 프리미엄 기능이 활성화되었습니다.',
    backgroundColor: AppColors.successMintSoft,
  );
  
  // 5. 홈으로 이동
  Get.back();
}
```

### 6. 친구 초대 연동 로직

**무료 이용권 보유 시**:
```dart
Future<void> checkFreeMonthCoupon() async {
  final storage = GetStorage();
  final hasFreeMonth = storage.read('invite_free_month') ?? false;
  
  if (hasFreeMonth) {
    // 안내 팝업 표시
    await Get.dialog(
      AlertDialog(
        title: Text('무료 이용권 확인'),
        content: Text(
          '보유하신 1개월 무료 이용권이 먼저 사용되고,\n'
          '그 이후에 결제가 시작됩니다.',
        ),
        actions: [
          TextButton(
            onPressed: () => Get.back(),
            child: Text('취소'),
          ),
          ElevatedButton(
            onPressed: () {
              Get.back();
              _proceedWithFreeMonth();
            },
            child: Text('진행하기'),
          ),
        ],
      ),
    );
  }
}

Future<void> _proceedWithFreeMonth() async {
  // 무료 1개월 적용 + 이후 선택한 플랜 자동 갱신
  final storage = GetStorage();
  storage.write('free_month_applied', true);
  storage.write('free_month_end_date', 
    DateTime.now().add(Duration(days: 30)).toIso8601String());
  
  // 프리미엄 활성화
  isPremiumUser.value = true;
}
```

---

## 📐 와이어프레임 (Wireframe)

```
┌─────────────────────────────────────┐
│  [← Back]                      [✕]  │
├─────────────────────────────────────┤
│                                     │
│   🐕 [Background Image: Sleeping]   │
│                                     │
│   반려동물에게 완벽한 평온을         │
│   선물하세요.                        │
│                                     │
│   ✓ 90개 전용 테라피 사운드 무제한   │
│   ✓ Smart Sync 잠금 해제            │
│   ✓ Haptic 무제한 사용              │
│                                     │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 👑 BEST VALUE               │   │
│  │ 1년 (Yearly)                │   │
│  │ ₩33,000 /년                 │   │
│  │ 월 2,750원 (30% 할인)        │   │
│  │ 🎁 7일 무료 체험             │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 6개월 (6 Months)            │   │
│  │ ₩19,900 /6개월 (15% Save)   │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 3개월 (3 Months)            │   │
│  │ ₩10,900 /3개월 (7% Save)    │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 1개월 (Monthly)             │   │
│  │ ₩3,900 /월                  │   │
│  └─────────────────────────────┘   │
│                                     │
├─────────────────────────────────────┤
│  💰 아직 결제가 망설여지시나요?      │
│     친구 초대하고 무료 이용권 받기 > │
├─────────────────────────────────────┤
│                                     │
│  ┌───────────────────────────────┐ │
│  │  7일 무료 체험 시작하기        │ │
│  └───────────────────────────────┘ │
│                                     │
│  7일 동안 요금이 청구되지 않습니다.  │
│  언제든 해지 가능합니다.             │
│                                     │
└─────────────────────────────────────┘
```

---

## 🎯 핵심 마케팅 포인트

### 무료 체험 강조
- "7일 무료" 텍스트를 여러 번 반복
- 리스크 없음 강조 ("언제든 해지 가능")

### 1년권 강조
- 골드 테두리, BEST VALUE 뱃지
- 30% 할인율 명확히 표시
- 월 환산 가격 강조 (₩2,750)

### 사회적 증명 (선택사항)
- "10,000명 이상의 보호자가 선택한 플랜"
- 실제 사용자 리뷰 간단히 표시

---

*작성일: 2025-11-28*
